# Customize the test machine
machine:

  # Version of ruby to use
  ruby:
    version:
      2.1.2

general:
  build_dir: atlasTesting

test:
  override:
    - gem install xamarin-test-cloud
    - test-cloud submit one.apk 414f8756808cc9e0bfb41d096f99c0c0 --devices f7d1b982 --series "master" --locale "en_US" --app-name "Sky Store TS" --user daljeet.sidhu@sky.uk