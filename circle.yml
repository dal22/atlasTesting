# Customize the test machine
machine:

  # Version of ruby to use
  ruby:
    version:
      2.1.2

test:
  override:
    - gem install xamarin-test-cloud
    - test-cloud submit "`pwd`/one.apk" 414f8756808cc9e0bfb41d096f99c0c0 --devices fcc074a4 --series "master" --locale "en_US" --user daljeet.sidhu@sky.uk